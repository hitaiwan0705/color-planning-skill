# ARCHITECTURE.md — 三層架構與介面契約

本檔定義「專業知識彙整／課程設計與作業設計／學術研究與實作應用」三者如何**分開維護**，
以及三者之間**唯一合法的介面**。

本檔是跨 repo 的協定。任一 repo 修改本檔，必須同步更新其他 repo 的副本。

---

## 1. 為什麼要分層：三種不同的變更頻率

分層的理由不是「檔案太多」，是**這三種東西以完全不同的節奏改變**。
綁在一起，任何一邊的更新都會污染另外兩邊的歷史。

| 層 | 代號 | 回答的問題 | 時效軸（什麼事會迫使它改） | 讀者 |
|---|---|---|---|---|
| 專業知識彙整 | **K** | 這個概念是什麼、標準怎麼定義 | 標準改版（ISO 3664:2009→2025）、論文發表 | 任何人，可公開 |
| 課程設計與作業設計 | **C** | 學生要做什麼、憑什麼算通過 | 學年、設備、證照年度、系上課程配置 | 授課者、助教、LLM |
| 學術研究與實作應用 | **R** | 這個介入有沒有效、如何證明 | 研究問題、IRB、投稿週期 | 研究者、審查者 |

**具體後果**：ISO 3664 在 2025 改版並移除螢幕觀察條件（見 `CONTEXT.md`）。
在單層架構下，這件事會同時觸動教材、作業判準與研究測量定義，且無法分辨哪一項是被動連帶修改。
分層後：K 層改一條 claim，C 層檢查哪些 `knowledge_refs` 指到它，R 層檢查是否影響已凍結的測量定義。

---

## 2. 依賴方向：嚴格單向

```
        R 研究層  ──依賴──▶  C 課程層  ──依賴──▶  K 知識層
     （研究問題）            （能力與交付物）        （概念與標準）

     ◀────────────  反向依賴一律違規，CI 阻擋  ────────────▶
```

**K 層不得知道課程存在。** 知識層檔案內出現課程或研究語彙
（學生、作業、評分、學號、本課、受試者、IRB、同意書）即為違規。
理由：一旦知識層寫進「本課要求學生……」，它就不能被第二門課、第三門課或外部使用者重用，
分層立刻退化成資料夾。

**C 層不得複製 K 層內容。** 課程層只能以 claim ID 指過去（見第 3 節）。
複製會產生第二個真相，而兩個真相必然漂移。

**R 層不得直接讀取 C 層的原始交付物。** 只能讀去識別化後的證據集（見第 4 節）。

---

## 3. 介面一｜K → C：claim ID

知識層的最小可引用單位是 **claim**，不是檔案、不是章節。
沒有穩定 ID，分層就無法被機器檢查。

```
`[CM-ICC-004 | tier: standard | src: https://... ]`
 └─ 領域碼      └─ 可信度等級    └─ 出處
```

**可信度等級（tier）決定它能被拿去做什麼** —— 這是整套架構最關鍵的一條：

| tier | 意義 | C 層可否用作評量判準 | R 層可否用作測量定義 |
|---|---|---|---|
| `standard` | 標準原文（ISO／CIE／ICC／W3C） | ✅ | ✅ |
| `peer_reviewed` | 同儕審查文獻 | ✅ | ✅ |
| `textbook` | 教科書 | ✅ 但須註明版本 | ❌ |
| `lecture_note` | 課堂筆記、投影片、非官方整理 | ❌ 僅得用於教學說明 | ❌ |
| `unverified` | 尚未查證 | ❌ | ❌ |

`ronnier-skill` 全庫屬於 `lecture_note`（它自己聲明未經授課教師審閱）。
因此它可以拿來**教**，不可以拿來**評分或當研究依據**。
這條界線若不寫死，遲早會有人用一份非官方筆記當作學生不及格的理由。

C 層的引用方式（寫進課程 repo 的 contract）：

```yaml
capabilities:
  - id: CAP-05
    knowledge_refs: [CM-ICC-004, CM-GMA-002]   # 必須存在且 tier 允許
    knowledge_pin: color-management-skill@<commit-sha>
```

**`knowledge_pin` 是必填的。** 課程層必須釘住知識層的某一個 commit。
否則知識層更新一條標準版本，去年的評量判準會被靜默改寫，學期中的評分基準就不可重現。

---

## 4. 介面二｜C → R：evidence ID 與去識別化閘門

課程層的 `evidence_contract` 已經為每件交付物定義了 artifact 名稱。
研究層只能透過**證據集導出**取得資料，且必須通過閘門：

```
C 層原始交付物 ──▶ [去識別化閘門] ──▶ R 層證據集
  含姓名/學號        雜湊化、欄位白名單      只有 evidence_id + 數值
```

閘門規則（沿用課程 repo 既有禁令，不放寬）：

1. 個資、學號、同意書內容**永不進入任何 repo**，包含 R 層。
2. 雜湊鹽值離線保管，不進 repo、不進 CI。
3. R 層的 `primary_outcome` 必須在資料蒐集**前**凍結，並記錄凍結時的 `knowledge_pin`。
4. `research_overlay.enabled` 的開關留在 C 層，R 層不得代為開啟。

---

## 5. repo 對應

| 層 | repo | 狀態 |
|---|---|---|
| K | `ronnier-skill` | 已存在。色彩科學／色度學，tier 全庫為 `lecture_note` |
| K | `color-management-skill` | **本 repo**。色彩管理，以 `standard` / `peer_reviewed` 為主 |
| C | `color-planning-skill` | 已存在。大三上 色彩規劃與傳播應用 |
| C | （未來）資訊視覺化設計 | 未建立。依既有原則，第二門課完成後才抽母規格 |
| R | （未來）`color-research-skill` | 未建立。研究問題與 IRB 確定前不建 |

**現在不要建 R 層 repo。** 研究問題尚未確定，先建只會產生一個猜測性的架構，
之後必然重寫。本檔已經把 R 層的介面定義好，等研究問題定案再落地即可。

---

## 6. 為什麼 `color-management-skill` 是獨立的 K 層 repo，而不是 `ronnier-skill` 的一部分

三個理由：

1. **可信度等級不同。** ronnier-skill 是非官方課堂筆記，本 repo 以標準原文為主。
   混在一起，tier 標示會失去意義。
2. **時效軸不同。** ronnier-skill 追的是「2021 年課程 vs 現況」的差異；
   本 repo 追的是標準版本本身的演進。
3. **內容缺口本來就在這裡。** ronnier-skill 的 20 篇筆記涵蓋色彩科學極深，
   但 ICC 架構、rendering intent、色域對映、分色與打樣驗證幾乎沒有覆蓋。

兩者是互補的 K 層 repo，不是上下級。C 層可以同時引用兩者，用 tier 決定用途。

---

## 7. 這套架構的失敗條件

以下任一情形出現，代表分層已經失效，必須停下來修架構而不是繼續加內容：

- K 層檔案出現課程或研究語彙 → `E-LAYER`
- C 層直接複製 K 層內文而非引用 claim ID → 兩個真相
- C 層引用了 tier 為 `lecture_note` / `unverified` 的 claim 作為評量判準 → `E-TIER`
- C 層沒有 `knowledge_pin` → 評分基準不可重現
- R 層在 `primary_outcome` 凍結前先看資料 → 研究效度歸零
