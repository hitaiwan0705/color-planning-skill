# CLAUDE.md

本檔為 Claude Code 與 Claude GitHub Action 的專案規則。
Codex 讀取的是 `AGENTS.md`，**兩份內容必須保持同步**；修改一份時同步修改另一份。

先讀 `CONTEXT.md`（已定案事實），再讀本檔（工作規則）。

---

## 這個 repo 是什麼

中國文化大學資訊傳播學系「色彩規劃與傳播應用」課程的 LLM 課程技能（skill）與其決定性工具。
目標有二：讓學生的作業成為業界看得懂的交付物；讓課程歷程可轉為研究證據。

**現階段只做這一門課。** 不建立其他課程目錄、不建立全系母規格。
待第二門課（資訊視覺化設計）完成後，才從兩門課抽取真正重複的部分。

---

## 絕對禁止

1. **任何學生個資、學號、同意書內容、研究資料進入 repo。** CI 會擋，但不要依賴 CI。
   需要範例時建立 `*.example.json`，欄位保留、值全部改為假資料。
2. **雜湊鹽值、API key、token 寫進任何檔案。** 一律走 GitHub Secrets 或本機環境變數。
3. **偽造測試資料。** 若無法取得權威參照值，明確標記缺口，不得依記憶生成數值填充。
4. **在無法執行程式碼的環境中自行估算色差、對比度或任何量化指標。**
   必須輸出：「本環境無法執行量化稽核，以下僅為方向性判斷，不得作為評分或研究依據」。

---

## 工作規則

### 提交
- 一個 PR 只處理一件事。跨越多個契約的變更請拆開。
- commit message 用祈使句，說明「為什麼」而非「改了什麼」（diff 已經說了改什麼）。
- 動到 `tools/` 必須同步更新對應測試，且測試要有**負向驗證**：
  植入對應錯誤後確認該測試會 FAIL。只會 PASS 的測試沒有價值。

### 事實主張
- 涉及外部事實（證照制度、標準版本、法規要求）的敘述，必須附可查證連結。
- 無法查證者標記 `<!-- UNVERIFIED: 需以 XXX 原始文件核對 -->`，不得寫成肯定句。
- **不確定時說不確定。** 本專案曾因「iPAS 沒有色彩職類」的錯誤斷言浪費一輪，
  該錯誤的成因是搜到單一來源就停止查證。涉及「某某不存在」的主張，一律反向查證。

### 審查
- 在 PR 留言 `@claude` 觸發審查。審查輸出分三級：
  `blocking`（不修不得合併）／`structural`（影響移植或研究效度）／`minor`。
- 審查針對 diff 與行號，不寫長篇評論文。修正建議一律用 GitHub suggestion 區塊。
- 與 Codex 意見衝突時，不要各自堅持——把分歧點寫成 PR 留言交由人類裁決。

---

## 目錄結構

```
CONTEXT.md              已定案事實（上限 150 行，由 check_file_budgets.py 執行）
CLAUDE.md / AGENTS.md   工作規則（本檔）
skill/color-planning/   課程 skill 本體
  SKILL.md              LLM 執行指令
  COURSE-CONTRACT.yaml  七項契約集中於此，不拆成多個 schema
tools/
  color/scripts/        決定性運算（色差、對比、色覺模擬）
  color/tests/          驗證套件（含負向驗證）
  validation/           repo 層防線：資料外洩、契約一致性、涵蓋範圍下限、檔案長度上限
  validation/tests/     防線自身的驗證套件（含負向驗證）
  materials/scripts/    教材確定性檢核
  materials/tests/      教材檢核器的驗證套件（含負向驗證）
```

**契約規則必須有執行者。** `global_rules.scope_floor` 曾經只寫在契約裡而沒有任何工具
在檢查，結果是靠人工比對官方簡章 PDF 才發現 14 條細目缺口。新增契約規則時，
一併問一句：**誰會執行它？** 沒有答案就是還沒寫完。

新增目錄層級前先在 PR 說明理由。架構膨脹是本專案的主要風險。

---

## 七項契約

1. 能力契約｜學生最後能獨立完成什麼
2. 任務契約｜在什麼情境、限制與角色下完成
3. 評量契約｜如何判斷品質與能力層級
4. 證據契約｜每個主張需要什麼證據
5. 工具契約｜哪些判斷必須由程式或儀器完成
6. 課程交接契約｜前後課程交換哪些具體產物
7. 平台可攜契約｜在不同 LLM 上如何維持最低一致性

全部集中在 `skill/color-planning/COURSE-CONTRACT.yaml`。

---

## 能力軸的處理原則

抽象能力動詞（understand / apply / integrate）**不得直接進入 skill**，
模型無法依它做任何判斷。進入 skill 前必須編譯成四項具體規格：

```
輸入證據 → 可觀察行為 → 交付物 → 驗證規則 → 失敗條件
```

例：不寫「學生能整合虛實輸出」，寫「學生須交出原始檔、輸出檔、實體樣本、
量測紀錄、ΔE 報告與修改紀錄；缺任一項即不通過」。

---

## 研究層的處理原則

研究資料是**可插拔的 overlay**，預設關閉：

```yaml
research_overlay:
  enabled: false
```

課程必要資料（任務版本、色彩空間、裝置與校正狀態、ICC profile、量測條件、
輸出結果、修改理由、評量證據、AI 使用揭露）不論是否研究都要保留。
研究附加資料（分組、primary outcome、前後測、同意狀態、匿名識別碼）
待研究問題與倫理程序確定後才啟用。

不要讓單一研究問題綁架課程設計。
