# MATERIALS-PLAN.md — 18 週教材製作施工圖

**這份文件是什麼**：教材製作階段的施工圖與迭代協定。
規定每一週要生產哪些檔案、由哪個設備站產出、審查到什麼程度算完成。

**這份文件不是什麼**：不是第八項契約。
契約規定「什麼算數」，本檔規定「怎麼做出來」。兩者衝突時一律以
`skill/color-planning/COURSE-CONTRACT.yaml` 為準，本檔不得放寬任一失敗條件。

**為什麼不併進 COURSE-CONTRACT.yaml**：契約檔的職責是判準。
把施工排程寫進去，會讓「這條是判準還是進度」變得不可分辨，審查也會失焦。

- 版本：`0.1.0-draft`
- 狀態：**待 Codex 審查**。審查通過前不得開始製作任何一週的教材。

---

## 1. 迭代流程協定

授課者指定的流程（2026-09-05）：

```
Claude 規劃  →  Codex 審查  →  Claude 製作  →  Codex 審核  →  Claude 修正  →  …
   本檔          第一輪          一次一包         逐包            逐條
```

### 1.1 迭代單位

**一輪處理一個「教材包」，不得整學期一次交付。**
教材包的粒度為：一個 week，或一個 TASK 的完整作業組。

理由：18 週一次交付會讓審查退化為抽樣，而抽樣審查抓不到跨週的證據鏈斷點。

### 1.2 各角色的依據

| 角色 | 讀什麼 | 產出什麼 |
|---|---|---|
| Claude 製作 | `CONTEXT.md`、`COURSE-CONTRACT.yaml`、`SKILL.md`、本檔 | 教材包檔案 + 自檢清單 |
| Codex 審核 | `AGENTS.md`、`COURSE-CONTRACT.yaml`、`CONTEXT.md`、本檔 | 三級審查意見（blocking／structural／minor） |
| Claude 修正 | Codex 意見 + 上列全部 | 逐條回應，不同意者不得沉默略過 |

### 1.3 一個教材包的完成定義（DoD）

七項全數成立才算完成，缺一即退回：

1. `COURSE-CONTRACT.yaml` 對應的 `deliverable` 逐項有對應教材支撐
2. 每個量化判斷都指到 `color_audit.py` 或 i1 Pro 量測檔，沒有語感判斷
3. 設備閘門已套用：`unverified`／`unavailable`／`undecided` 項目一律標 BLOCKED
4. 外部事實有可查證連結，查不到的標 `<!-- UNVERIFIED: ... -->`
5. 範例資料全部為假資料（`*.example.json`），無任何真實個資
6. 該包的失敗條件寫成可判定的句子，不是「應注意」
7. Codex 的 blocking 意見為零

### 1.4 意見分級與處置

| 級別 | 定義 | 處置 |
|---|---|---|
| `blocking` | 違反契約、閘門或禁止事項 | 不修不得合併 |
| `structural` | 影響移植、下游交接或研究效度 | 修，或寫明不修的理由 |
| `minor` | 用字、排版、可讀性 | 可批次處理 |

**與 Codex 意見衝突時，不各自堅持**——把分歧寫成 PR 留言交由授課者裁決
（`CLAUDE.md` 審查節）。

---

## 2. 教材包的統一結構

每一週產出一個目錄，結構固定。固定結構是為了讓審查可以逐項比對，
而不是每週重新理解一次檔案佈局。

```
materials/W##_<主題>/
  00-lesson-outline.md      課堂敘事線：先講產業問題，再給定義與流程
  01-demo-script.md         示範腳本：逐步驟、每步驟寫出預期畫面與失敗徵狀
  02-student-worksheet.md   操作單：學生逐格填寫，含必填欄位與單位
  03-checklist.md           交件前自檢表，對應該週 deliverable 逐項打勾
  04-common-errors.md       常見錯誤與其徵狀（寫徵狀，不只寫「不要這樣」）
  05-example-data/          假資料範例（*.example.json、量測檔樣板）
```

**四項寫作規則**：

1. 操作單的每個欄位都要有單位與來源欄。沒有來源欄的數字，事後無法追溯。
2. 示範腳本要寫「失敗徵狀」。只寫正確步驟的腳本，學生卡住時無法自救。
3. 常見錯誤寫可觀察的徵狀（畫面、數值、檔案），不寫抽象叮嚀。
4. 不得使用抽象能力動詞（理解／應用／整合／熟悉／掌握），一律寫成可觀察行為。

---

## 3. 一條龍設備流程

授課者要求教材盡量結合系上設備。以下是九站鏈路，
**每一站都必須產出一個可被下一站消費的檔案**——沒有產出檔的站，就是無法查核的站。

| 站 | 內容 | 設備 | 產出檔 | 閘門 |
|---|---|---|---|---|
| S1 | 數位設計與色票登錄 | — | `design_token_table.json`、原始設計檔 | — |
| S2 | 顯示器校正與特性化 | i1 Pro 2／3 + i1Profiler | `i1Profiler_calibration_record`、OS 層 ICC | ⚠️ 僅 software calibration；**不得寫入 monitor LUT** |
| S3 | 指定工作色域 | — | `working_space_setting` | — |
| S4 | 軟打樣與色域外清單 | 顯示器 + 媒材 profile | `soft_proof_setting`、`out_of_gamut_color_list` | — |
| S5 | 媒材／設備描述檔登錄 | Mimaki UJF-3042FX | `media_device_profile_record` | ⚠️ 媒材清單未確認 → BLOCKED |
| S6 | 實體輸出（第一次） | Mimaki UJF-3042FX | `first_physical_sample` | ⚠️ 300×420 mm、厚度 50 mm 為硬上限；白墨／clear 未確認 → BLOCKED |
| S7 | 標準燈箱視覺評估 | D50／D65 燈箱 | `D50_D65_light_booth_observation` | ⚠️ 型號等級標「待核對」；**不得假設 TL84／CWF／U30** |
| S8 | 儀器量測與色差 | i1 Pro 2／3 | `i1_pro_measurement_file`、`DeltaE00_report` | 必註明 CIEDE2000，不得與 CIE76 混用 |
| S9 | 修正再輸出與前後比較 | Mimaki + i1 Pro | `revised_physical_sample`、`DeltaE00_before_after_report` | 同 S6 |

### 3.1 關於「校色器」

授課者提到校色器。**`CONTEXT.md` 第 2 節沒有獨立的色度計**——
已定案的校色路徑是 **i1 Pro 2／i1 Pro 3 + i1Profiler → OS 層 ICC profile**，
並明確記載「不需另購色度計」。

因此教材中的「校色」一律指 S2 這條路徑。
<!-- 待授課者確認：系上若另有色度計（如 i1Display 系列），須先寫入 CONTEXT.md
     第 2 節並註明型號與數量，才可排入教材。在此之前不得出現於任何教材檔。 -->

### 3.2 這條鏈在 18 週中的位置

S1–S4 在 week 3、7、8 逐站教到；S5–S9 依賴 TASK-06，
而 TASK-06 目前未排入週次（見 `COURSE-CONTRACT.yaml` 的 `weekly_plan.unscheduled_task`）。

**因此完整一條龍的教材，在 TASK-06 的三選項裁決之前不得開工。**
可先製作的是 S1–S4 的教材包（week 3、6、7、8）。

---

## 4. 作業教材的對應

作業本身的判準已在契約，本節只規定「教材要生產什麼來支撐它」。

| 任務 | 週次 | 教材包須額外產出 |
|---|---|---|
| TASK-02 | W03→W04 | token 表樣板（含唯一 ID／色彩空間／來源檔／profile 版本欄）、跨空間比較的警示範例 |
| TASK-03 | W04→W07 | 意象決策表樣板、排除理由的寫法範例（兩則合格、兩則不合格對照） |
| TASK-04 | W06→W08 | 量測條件表樣板、i1 Pro 操作腳本、異常值處理決策樹 |
| TASK-05 | W07→W09 | i1Profiler 逐步截圖腳本、profile 版本命名規則、軟打樣設定對照表 |
| TASK-07 | W10→W16 | P1–P7 每週一張新增欄位的資料表樣板、機制歸因的排除理由範例 |
| TASK-01 | W14→W16 | SC 1.4.1 人工檢核表、`color_audit.py` cvd 輸出判讀範例 |
| TASK-08 | W16→W18 | 報告結構樣板（問題／方法／觀察條件／資料／限制）、限制章節的合格範例 |

**P1–P7 的資料表必須是同一張表逐週長出來的**，不是七張獨立表格。
教材要提供的是「本週新增哪幾欄」，不是「本週重做一張」。

---

## 5. 評分方式（草案，待授課者核定）

`COURSE-CONTRACT.yaml` 的 `assessment_contract` 規定 skill 只輸出證據落差清單、
不輸出分數。**本節是給授課者用的規準，不是給 skill 用的。**
skill 仍然不得產生分數。

### 5.1 先過門檻，再評分

門檻不是權重維度。缺件就是缺件，不能用其他項目補償
（`evidence_contract.completeness_rule`：`compensation_allowed: false`）。

| 門檻 | 內容 | 未過的後果 |
|---|---|---|
| G1 | `required_artifacts.always` 齊全 | 該次不計分，退回補件並記錄遲交 |
| G2 | AI 使用揭露存在 | 同上（`disclosure_missing`） |
| G3 | 每個數值主張可追溯至工具輸出或儀器檔 | 該數值不採計（`unsupported_claim`） |

### 5.2 兩套權重（過門檻後）

**R-A｜製作與管理型任務**（TASK-02、04、05，以及 TASK-06 若開）

| 維度 | 權重 | 可觀察標準 |
|---|---|---|
| 可追溯性與量測紀錄品質 | 25% | 條件欄位齊全、單位正確、儀器型號與日期完整 |
| 規格符合度 | 25% | 命名、版本、色彩空間宣告、profile 記錄符合樣板 |
| 判斷的正當性 | 25% | 取捨有理由，且寫出排除其他選項的依據 |
| 修正迴圈證據 | 25% | 第一次與修正後的對照，含修改理由 |

**R-B｜觀察與報告型任務**（TASK-01、07、08）

| 維度 | 權重 | 可觀察標準 |
|---|---|---|
| 觀察條件紀錄完整性 | 25% | 光源、背景、距離、適應狀態、時間 |
| 機制歸因與排除理由 | 30% | 指名單一機制，並寫出排除其餘機制的依據 |
| 資料與工具輸出的一致性 | 25% | 人眼判讀與儀器數值並列，不一致處有說明 |
| 限制的誠實度 | 20% | 明確列出本次資料無法回答的部分，不得為空 |

### 5.3 學期配分草案

| 任務 | 配分 |
|---|---|
| TASK-02 色彩表徵與 token | 10 |
| TASK-03 色彩意象決策 | 10 |
| TASK-04 量測與 ΔE00 報告 | 15 |
| TASK-05 顯示器校正與軟打樣 | 15 |
| TASK-07 感知落差資料集（P1–P7） | 25 |
| TASK-01 色彩知覺稽核 | 10 |
| TASK-08 自主學習報告 | 15 |
| **合計** | **100** |

<!-- 待授課者裁決：
     1. 若 TASK-06 採 OPT-A 或 OPT-B，本表須重新配分（TASK-06 為最重的實作，
        不宜低於 20 分，將壓縮 TASK-07 或 TASK-05）。
     2. P1–P7 的 25 分如何切分：等分 7 份，或前輕後重。
     3. 遲交與補件的扣分規則尚未定義。 -->

### 5.4 三條禁止

1. skill 不得輸出分數、等第或總評語——分數由授課者裁決。
2. 不得以其他項目品質良好，補償缺件。
3. 不得在無法執行程式碼的環境中估算任何量化指標作為評分依據。

---

## 6. 未決事項（會擋住製作）

| # | 事項 | 擋住什麼 |
|---|---|---|
| 1 | TASK-06 三選項裁決 | S5–S9 全部教材，以及配分表 |
| 2 | Mimaki 白墨／clear 是否已安裝 | S5、S6 的媒材教材 |
| 3 | 燈箱型號與螢光燈管種類 | S7 的觀察條件教材、同色異譜演示 |
| 4 | 授課地點與設備配置 | 所有需要固定教室的操作單 |
| 5 | 開學日 | week 與日期對應、11/14 落在第幾週的核算 |
| 6 | 客座講者的可授課週次與講題 | week 2、week 5 的教材包 |
| 7 | 系上是否另有色度計 | 3.1 的校色路徑敘述 |

**第 1、5 項未決前，製作只能進行到 week 3、6、7、8 的 S1–S4 教材包。**
