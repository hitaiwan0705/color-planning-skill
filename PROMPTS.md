# PROMPTS.md — 驅動 Codex 與 Claude Code 的指令集

本檔存放可直接複製貼上的 prompt。放進 repo 一起版控，改良後就地更新。

## 使用順序

```
P0  Claude Code  補完 CONTEXT.md          （必須先做，其他全部依賴它）
P1  Claude Code  建立 skill 骨架          （只做結構，不寫課程內容）
P2  Codex        填入課程實質內容          （開 PR）
P3  Claude Code  審查 Codex 的 PR
P4  Codex        審查 Claude 的 PR
P5  任一方       卡住或偏離時的糾正指令
```

**不要讓兩個 agent 同時動同一個分支。** 一次一個，透過 PR 交棒。

---

## P0｜Claude Code：補完 CONTEXT.md

> 在本機 repo 根目錄執行 `claude`，貼入以下內容。

```
先讀 CONTEXT.md 與 CLAUDE.md，然後只做一件事：把 CONTEXT.md 裡所有
<!-- 待填 --> 的欄位補完。

規則：
1. 這些欄位你不可能知道答案，也不准猜。一次問我一組相關問題，
   我回答後你才寫入。問題要具體到我能直接回答，例如
   「燈箱型號是什麼？可切換哪些光源（D50/D65/A/TL84）？」
   而不是「請提供設備資訊」。
2. 設備欄位優先，因為它決定課程能做到什麼程度。
3. 我如果說「還不確定」，就在該欄位寫 <!-- 未定 -->，不要編一個合理值填進去。
4. CONTEXT.md 上限 150 行。若補完後超過，告訴我哪些內容應該搬走，
   由我決定，不要自己刪。
5. 全程不要建立任何新檔案，不要修改 CONTEXT.md 以外的檔案。

完成後只回報三件事：填了幾欄、還有幾欄未定、行數是多少。不要寫總結。
```

**驗收**：CONTEXT.md 沒有 `<!-- 待填 -->`（只剩 `<!-- 未定 -->`），行數 ≤ 150，
且沒有其他檔案被改動（`git status` 只顯示 CONTEXT.md）。

---

## P1｜Claude Code：建立 skill 骨架

> CONTEXT.md 完成並合併後才執行。

```
先讀 CONTEXT.md 與 CLAUDE.md。

任務：建立 skill/color-planning/SKILL.md 與 skill/color-planning/COURSE-CONTRACT.yaml
的「骨架」。骨架的定義是：欄位結構、必填規則、驗證邏輯、失敗條件都寫完整，
但課程的實質內容留空並標記 TODO(codex)。

因為課程內容（六層結構的實際教學活動、量測流程、Mimaki 輸出鏈、
前後課交接物）需要領域判斷，那部分由 Codex 依 CONTEXT.md 填入。

COURSE-CONTRACT.yaml 必須包含七項契約且集中在同一份檔案，不准拆成多個 schema：
能力、任務、評量、證據、工具、課程交接、平台可攜。

平台可攜契約必須明確定義降級規則，至少涵蓋：
- 無程式碼執行環境：不得計算研究等級指標，須提供可重現的公式或腳本，
  結果標記為 unverified
- 無視覺輸入：要求數值色彩資料，不得宣稱已目視檢查
- 無檔案存取：只索取最小必要片段，不得宣稱已審閱完整檔案

研究層必須是可插拔 overlay，預設 research_overlay.enabled = false。

硬性限制：
- 不准新增 SKILL.md 與 COURSE-CONTRACT.yaml 以外的任何檔案
- 不准建立其他課程的目錄
- 不准寫抽象能力動詞（understand / apply / integrate）。所有能力描述必須編譯成
  「輸入證據 → 可觀察行為 → 交付物 → 驗證規則 → 失敗條件」五段式
- 涉及外部事實的敘述必須附可查證連結；查不到就標 <!-- UNVERIFIED: 需以 X 核對 -->
- SKILL.md 上限 400 行

開一個分支 skeleton/skill-and-contract，完成後 push 並開 PR。
PR 說明依 .github/pull_request_template.md 的格式填寫。
```

**驗收**：只有兩個新檔案；`grep -c "TODO(codex)"` 有明顯數量（代表真的留空給
Codex 而非自己編）；沒有出現 understand/apply/integrate 這類動詞；CI 綠燈。

---

## P2｜Codex：填入課程實質內容

> 在 Codex（cloud 或 CLI）對該 repo 執行。

```
先讀 CONTEXT.md 與 AGENTS.md，再讀 skill/color-planning/ 下的兩份檔案。

任務：把所有 TODO(codex) 標記處填入課程實質內容。內容依 CONTEXT.md 第 3 節的
六層結構（色彩知覺 / 表徵 / 意象 / 量測 / 管理 / 虛實輸出），並確保：

1. 課程涵蓋範圍不得比 iPAS 色彩規劃管理師初級三科更窄
   （色彩學、色彩計畫實務、色彩管理）
2. 虛實輸出必須是完整鏈：數位設計 → 顯示器校正 → 指定工作色域 → 軟打樣 →
   媒材/設備描述檔 → 實體輸出 → 標準燈箱觀察 → 儀器量測 → 視覺評估 → 修正再輸出
3. 課程交接契約要寫出與「資訊視覺化設計」之間**具體的**交接產物，
   不是能力描述。例如「色彩課交出 design token 表與 ΔE 報告，
   資視課讀取後用於視覺編碼決策」
4. 只寫 CONTEXT.md 列出的設備做得到的事。CONTEXT.md 標 <!-- 未定 --> 的設備，
   相關教學活動一律標 <!-- BLOCKED: 待設備確認 -->，不要假設它存在

硬性限制：
- 不准新增檔案，不准修改 tools/ 與 tests/ 下的任何東西
- 不准修改骨架的欄位結構。若你認為結構有問題，在 PR 說明裡提出，
  由人類裁決，不要自行改動
- 不准寫抽象能力動詞。所有能力描述用五段式：
  輸入證據 → 可觀察行為 → 交付物 → 驗證規則 → 失敗條件
- 涉及外部事實必須附連結；查不到標 <!-- UNVERIFIED -->，不得寫成肯定句
- 不准決定研究問題或 primary outcome，research_overlay 維持 enabled: false

開分支 content/course-substance，完成後開 PR，並在 PR 留言 @claude 請求審查。
```

**驗收**：`git diff --stat` 只動到那兩個檔案；沒有殘留 TODO(codex)；
設備相關內容與 CONTEXT.md 一致（沒有憑空出現的儀器）。

---

## P3｜Claude Code 審查 Codex 的 PR

> 直接在 PR 留言區貼。

```
@claude 依 CLAUDE.md 審查本 PR。只回報三類問題，其餘略過：

1. blocking — 事實錯誤、無來源的肯定句、超出 CONTEXT.md 設備範圍的教學活動、
   結構被擅自改動、抽象能力動詞
2. structural — 影響跨課移植或未來研究效度的設計問題
3. minor — 措辭與格式

規則：
- 每一項都用 inline comment 標在具體行號上，修正建議用 GitHub suggestion 區塊
- 不要寫總結長文
- 不要因為「內容看起來合理」就放行事實主張。凡是肯定句就要有來源
- 如果你自己也不確定某個事實，寫出來，不要為了看起來權威而下判斷
- 沒有 blocking 問題時就直接說沒有，不要為了顯示有在做事而硬找問題
```

---

## P4｜Codex 審查 Claude 的 PR

```
@codex review

依 AGENTS.md 審查。特別檢查三件事：

1. 是否有超出本輪範圍的擴充——新增檔案、新增目錄層級、預建其他課程結構。
   本專案的主要風險是架構膨脹，發現即標 blocking
2. 是否有無來源的事實主張。Claude 曾因單一來源查證不足而出錯，
   涉及「某某不存在」「某某是唯一」的斷言一律要求反向查證
3. tools/ 下的變更是否有對應測試，且測試是否經負向驗證
   （植入錯誤後確認會 FAIL）。只會 PASS 的測試視為無效

用 inline comment，不要寫評論文。
```

---

## P5｜偏離時的糾正指令

> 任一方開始擴充架構、寫長文、或編造內容時貼這段。

```
停。你正在做本專案明確禁止的事。

重讀 CONTEXT.md 第 6 節的十條工作原則，然後回答我三個問題：
1. 你這一輪新增了幾個檔案？授權在哪裡？
2. 你寫下的哪些肯定句沒有可查證來源？
3. 你有沒有為了讓輸出看起來完整，填入你其實不確定的內容？

先回答，不要繼續動手。回答後等我指示。
```

---

## 常用短指令

| 情境 | 貼這段 |
|---|---|
| 想知道現在狀態 | `讀 CONTEXT.md 與 git log，用 5 行內告訴我現在卡在哪一步、下一步該做什麼。不要建議新功能。` |
| 工具改動後 | `為這次改動補上測試，並做負向驗證：植入對應錯誤，證明測試會 FAIL。貼出兩次執行的實際輸出。` |
| 覺得回答太漂亮 | `你剛才的回答裡，哪一句是你查證過的，哪一句是你推測的？逐句標示。` |
| 準備收尾 | `跑完整 CI（測試 + 資料防線），貼出實際輸出。有任何紅燈就停下來報告，不要自己修。` |

---

## 給人類的提醒

您是裁決者，不需要讀完所有輸出。每輪只看三件事：

1. **CI 是綠是紅**——這取代所有「已驗證」的口頭宣稱
2. **`git diff --stat` 動了幾個檔案**——超出授權範圍就退回
3. **有沒有出現 `<!-- UNVERIFIED -->`**——有，代表 agent 誠實；
   一個都沒有反而要懷疑它是不是把不確定的事寫成了肯定句

第 3 點反直覺但重要：**完全沒有不確定標記的輸出，通常不是因為全部查證過，
而是因為 agent 沒在區分。**
